name: First GitHub Action

on:
  issues:
    types:
      - opened
jobs:
  say-hello:
    runs-on: ubuntu-latest
    steps:
      - name: Print a message
        run: echo "🎉 Hello from GitHub Actions!"
